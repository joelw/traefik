server {
    listen 80;
    server_name _;

    # Return a simple JSON response for every path so test assertions are easy.
    location / {
        default_type application/json;
        return 200 '{"status":"ok","path":"$request_uri"}';
    }
}
