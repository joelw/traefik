server {
    listen 80;
    server_name _;

    # Fixed-size response body keeps benchmark variance low.
    location / {
        default_type application/json;
        return 200 '{"status":"ok","path":"$request_uri"}';
    }
}
